%li{ class: ('active' if current_admin_path.start_with?('/articles')) }
  = link_to spina.admin_articles_path do
    = t('spina.articles.title')

%li{ class: ('active' if current_admin_path.start_with?('/categories')) }
  = link_to spina.admin_categories_path do
    = t('spina.categories.title')
